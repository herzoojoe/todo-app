# 오늘의 할 일 — PRD (Product Requirements Document)

## 1. 개요

### 1.1 제품 목적
하루 동안 할 일을 간단히 적고 체크하는 개인용 웹 앱. 설치나 회원가입 없이 파일을 열면 바로 사용할 수 있고, 브라우저를 닫았다 열어도 목록이 유지되는 가벼운 도구.

### 1.2 대상 사용자
컴퓨터에 익숙하지 않은 일반인 — 복잡한 설정 없이 직관적으로 바로 쓸 수 있어야 함.

---

## 2. 범위

### 2.1 In Scope (이번 버전에 포함)
| 기능 | 설명 |
|---|---|
| 할 일 추가 | 입력 후 Enter 또는 추가 버튼 클릭으로 목록에 추가 |
| 완료 체크 | 체크하면 해당 항목에 취소선 표시 |
| 할 일 삭제 | 개별 항목 삭제 |
| 할 일 수정 | 항목 더블클릭 시 텍스트 수정 가능 |
| 필터 | 전체 / 진행 중 / 완료 3종 필터로 목록 보기 전환 |
| 남은 할 일 개수 표시 | 아직 완료하지 않은 항목 수 표시 |
| 완료 항목 하단 정렬 | 완료 체크한 항목은 목록 맨 아래로 이동 |
| 빈 목록 안내 문구 | 표시할 항목이 없을 때 "오늘 할 일을 추가해 보세요 ✏️" 안내 표시 |
| 완료 항목 일괄 삭제 | "완료된 항목 모두 지우기" 버튼, 클릭 시 확인 후 삭제 |
| 데이터 유지 | `localStorage`에 저장, 브라우저를 닫았다 열어도 목록 유지 |

### 2.2 Out of Scope (이번 버전 제외)
- 회원가입/로그인
- 여러 사람과 공유 (서버 연동, 클라우드 동기화 포함)
- 알림/리마인더 기능

---

## 3. 사용자 스토리

1. 사용자로서, 할 일을 입력하고 Enter나 버튼 클릭으로 빠르게 추가하고 싶다.
2. 사용자로서, 완료한 항목을 체크해서 한눈에 구분하고 싶다.
3. 사용자로서, 더 이상 필요 없는 할 일을 삭제하고 싶다.
4. 사용자로서, 잘못 입력한 할 일을 더블클릭해서 바로 수정하고 싶다.
5. 사용자로서, 전체/진행 중/완료 항목만 골라서 보고 싶다.
6. 사용자로서, 오늘 얼마나 남았는지 개수로 바로 확인하고 싶다.
7. 사용자로서, 완료한 항목은 아래로 내려가서 아직 안 한 일이 위에 잘 보이길 원한다.
8. 사용자로서, 할 일을 다 지웠을 때 빈 화면 대신 안내 문구를 보고 싶다.
9. 사용자로서, 완료한 항목들을 한 번에 정리하고 싶고, 실수로 다 지우지 않도록 확인을 받고 싶다.
10. 사용자로서, 브라우저를 껐다 켜도 입력한 목록이 그대로 남아있길 원한다.

---

## 4. 기능 상세 요구사항

### 4.1 할 일 추가
- 텍스트 입력창 + 추가 버튼
- Enter 키로도 추가 가능
- 빈 텍스트는 추가되지 않도록 검증

### 4.2 완료 체크
- 항목 좌측 체크박스로 완료/미완료 토글
- 완료 항목은 취소선으로 시각적 구분

### 4.3 할 일 삭제
- 항목별 삭제 버튼 제공, 클릭 시 즉시 목록에서 제거

### 4.4 할 일 수정
- 항목 텍스트를 더블클릭하면 인라인 입력창으로 전환되어 수정 가능
- Enter 또는 포커스 아웃 시 수정 내용 저장
- 빈 텍스트로 수정 시 저장되지 않음 (기존 텍스트 유지)

### 4.5 필터
- 상단에 전체 / 진행 중 / 완료 탭 제공
- 선택한 필터에 맞는 항목만 목록에 표시

### 4.6 남은 할 일 개수 표시
- "N개 남음" 형태로 화면에 표시
- 완료 처리/삭제 시 즉시 갱신

### 4.7 데이터 저장 (영속성)
- 추가/완료/삭제 동작 시 `localStorage`에 즉시 저장
- 페이지 로드 시 `localStorage`에서 불러와 렌더링
- 저장된 데이터가 없으면 빈 목록으로 시작

### 4.8 완료 항목 하단 정렬
- 완료 체크한 항목은 목록에서 자동으로 맨 아래로 이동
- 미완료 항목끼리, 완료 항목끼리는 각각 추가한 순서를 유지
- 완료 체크를 해제하면 다시 미완료 그룹(원래 순서)으로 돌아감

### 4.9 빈 목록 안내 문구
- 현재 필터 조건에 맞는 항목이 하나도 없을 때 목록 영역에 "오늘 할 일을 추가해 보세요 ✏️" 문구 표시
- 항목이 하나라도 있으면 문구는 사라짐

### 4.10 완료 항목 일괄 삭제
- "완료된 항목 모두 지우기" 버튼 제공
- 클릭 시 확인창(예: `confirm()`)을 띄워 재확인
- 확인 시 완료된 항목만 모두 삭제, 취소 시 아무 변화 없음

---

## 5. 데이터 모델 (예시)

```js
// localStorage key: "todos"
[
  {
    id: "uuid-or-timestamp",
    text: "우유 사기",
    completed: false
  }
]
```

---

## 6. 기술 요구사항

- **언어/스택**: 순수 HTML + CSS + Vanilla JavaScript (프레임워크, 서버, 로그인 없음)
- **저장소**: `localStorage` (브라우저 내장, 별도 백엔드 불필요)
- **반응형**: 휴대폰 화면에서도 보기 좋게 (모바일 우선 또는 모바일 대응 반응형 레이아웃)

---

## 7. 화면 구성 (와이어프레임 개념)

```
┌───────────────────────────────┐
│  오늘의 할 일                  │
├───────────────────────────────┤
│  [입력창................] [추가] │
├───────────────────────────────┤
│  [전체] [진행 중] [완료]        │
├───────────────────────────────┤
│  ☐ 우유 사기            [삭제]  │
│  ☐ 운동하기             [삭제]  │
│  ☑ ~~보고서 제출~~      [삭제]  │  ← 완료 항목은 아래로
├───────────────────────────────┤
│  2개 남음   [완료된 항목 모두 지우기] │
└───────────────────────────────┘

(할 일이 없을 때)
┌───────────────────────────────┐
│  오늘의 할 일                  │
├───────────────────────────────┤
│  [입력창................] [추가] │
├───────────────────────────────┤
│  [전체] [진행 중] [완료]        │
├───────────────────────────────┤
│      오늘 할 일을 추가해 보세요 ✏️  │
├───────────────────────────────┤
│  0개 남음   [완료된 항목 모두 지우기] │
└───────────────────────────────┘
```

---

## 8. 완료 기준 (Acceptance Criteria)

- [ ] 입력창에 텍스트를 쓰고 Enter 또는 추가 버튼을 누르면 목록에 즉시 추가된다
- [ ] 빈 텍스트는 추가되지 않는다
- [ ] 체크박스를 클릭하면 해당 항목에 취소선이 표시되고, 다시 클릭하면 해제된다
- [ ] 삭제 버튼을 클릭하면 해당 항목이 목록에서 즉시 사라진다
- [ ] 할 일 텍스트를 더블클릭하면 수정 모드로 전환되고, 수정 후 저장하면 목록에 반영된다
- [ ] 전체/진행 중/완료 필터를 클릭하면 그에 맞는 항목만 표시된다
- [ ] 남은 할 일 개수가 완료 처리/삭제할 때마다 정확히 갱신된다
- [ ] 완료 체크한 항목은 목록 맨 아래로 이동하고, 체크 해제하면 원래 순서로 돌아간다
- [ ] 표시할 항목이 없을 때 "오늘 할 일을 추가해 보세요 ✏️" 문구가 나타나고, 항목이 생기면 사라진다
- [ ] "완료된 항목 모두 지우기" 클릭 시 확인창이 뜨고, 확인해야만 완료 항목이 삭제되며 취소하면 삭제되지 않는다
- [ ] 새로고침하거나 브라우저를 껐다 켜도 목록과 완료 상태가 그대로 유지된다
- [ ] 휴대폰 화면 크기에서도 레이아웃이 깨지지 않고 버튼과 텍스트를 누르기 편하다
- [ ] 회원가입, 공유, 알림 관련 기능이 전혀 없다
- [ ] HTML, CSS, JavaScript만으로 동작하며 별도 프레임워크나 서버가 필요 없다
